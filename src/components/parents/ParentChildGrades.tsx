import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ParentChildGrades = (): JSX.Element => {
	return (
		<View>
			<Text>ParentChildGrades</Text>
		</View>
	);
};

const styles = StyleSheet.create({});

export default ParentChildGrades;
