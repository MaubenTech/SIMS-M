import { View, Text } from "react-native";
import React from "react";

const GroupScreen = (): JSX.Element => {
	return (
		<View>
			<Text>GroupScreen</Text>
		</View>
	);
};

export default GroupScreen;
