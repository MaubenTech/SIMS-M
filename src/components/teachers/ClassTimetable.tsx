import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ClassTimetable = (): JSX.Element => {
	return (
		<View>
			<Text>Timetable</Text>
		</View>
	);
};

const styles = StyleSheet.create({});

export default ClassTimetable;
