import { StyleSheet, Text, View } from "react-native";
import React from "react";

const TeacherFormClass = (): JSX.Element => {
	return (
		<View>
			<Text>TeacherFormClass</Text>
		</View>
	);
};

const styles = StyleSheet.create({});

export default TeacherFormClass;
